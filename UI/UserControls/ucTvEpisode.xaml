<UserControl x:Class="UI.UserControls.ucTvEpisode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock FontSize="10pt" Grid.Column="1">
            <TextBlock.Inlines>
                <Run Text="{Binding EpisodeNumber}" />
            </TextBlock.Inlines>
        </TextBlock>

        <TextBlock Grid.Column="2" Text="{Binding HasBeenWatched}" FontSize="10pt" />
    </Grid>
</UserControl>
